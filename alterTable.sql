/*Apontando uma coluna já existente como primary key*/
ALTER TABLE nomeTabela ADD PRIMARY KEY (nome da coluna que deseja ser a primary key);

/*Acrescentando uma coluna numa tabela já existente*/
ALTER TABLE nomeTabela ADD COLUMN (nomeDaColuna tipoDaColuna)
